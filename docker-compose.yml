version: '3.8'

services:
  backend:
    build:
      context: backend
      dockerfile: Dockerfile
    command: ['uvicorn', 'src.main:app', '--host', '0.0.0.0', '--port', '8000']
    ports:
      - '8000:8000'
    env_file: .env
